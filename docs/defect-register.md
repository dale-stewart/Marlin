# Defect register

Behaviour found while rescuing code, recorded rather than changed. Every entry is
pinned by a test carrying a `LEGACY-BEHAVIOR:` comment, so the current behaviour cannot
drift unnoticed — and so that fixing one starts from a failing test rather than a
guess.

Nothing here has been fixed, with three recorded exceptions (#16, #18 and #20) where the
defect was in the measuring instrument itself. Changing any of the rest changes what the
firmware does, which is a decision for the maintainer, not a side effect of adding tests.

**Status values:** `open` — decision needed · `by-design` — recorded as intended
· `blocked` — fix requires work that is not yet safe.

## Genuine defects

| # | Symptom | Where | Impact | Status |
|---|---|---|---|---|
| 1 | `i16tostr3left()` cannot represent a negative. Digits come from `'0' + (n % 10)`, so `-5` renders as `"+"` (ASCII 43) and `-1` as `"/"`. | `libs/numtostr.cpp:143` | Punctuation on the display instead of a number. Ten call sites (DOGM, TFTGLCD, tft, extui, menu_info) all pass non-negative values today, so latent. The signature takes `int16_t` and promises otherwise. | open |
| 2 | `ftostr42_52(99.999)` returns `"00.00"`. The dispatch tests the value *before* rounding, picks the narrow field, then rounding carries to 100.00 and the leading digit is dropped. | `libs/numtostr.cpp:225` | A value just under a hundred displays as zero. | open |
| 3 | `i16tostr4signrj(-1000)` returns `"-000"`. The four-digit branch is chosen on the signed value, then the sign overwrites the thousands digit. | `libs/numtostr.cpp:155` | Reads as zero rather than a thousand. | open |
| 4 | `Stopwatch::resume()` zeroes `startTimestamp` via `reset()`, then sets `RUNNING` without setting it again, so `duration()` computes `millis() - 0`. | `libs/stopwatch.cpp:82` | A print resumed after power loss is reported as older than it is, by the controller's uptime. The error is an **offset, not a drift**: `millis()` is both the uptime and the origin the elapsed time is measured from, so the clock still advances one second per second — it is simply too high for ever, by the uptime at the instant of the resume. That is why nobody watching the number climb would notice. Affects print time display and remaining-time estimates. Now reachable end to end and pinned through the feature that causes it, by `power_loss___a_resumed_job_reports_the_uptime_as_well_as_the_resumed_time` — `PrintJobRecovery::resume()` issues `M24 T<seconds>`, and `M24` hands that straight to `Stopwatch::resume()`. | open |
| 5 | `Stopwatch::resume(0)` leaves the watch stopped — `if ((accumulator = with_time)) state = RUNNING;` only starts on a non-zero time. | `libs/stopwatch.cpp:86` | A job resumed with zero accumulated time runs untimed. Power-loss recovery normally restores a non-zero time, so latent. | open |
| 6 | `GCodeParser::parse()` assigns `string_arg` after skipping spaces, so for a valueless parameter followed by another token it points at the space before the *next* token: `"G0 X Y"` yields `" Y"`, not `"X Y"`. | `gcode/parser.cpp:348` | The string argument is off by one token. `"G0 X"` is unaffected. M-code string handling may depend on the current shape. | open |
| 7 | `i8tostr3rj(-128)` returns `"-28"` — the sign takes the hundreds column and the hundreds digit is lost. | `libs/numtostr.cpp:81` | Only affects the single value `-128`. | open |
| 8 | A stray continuation byte (a sequence starting mid-character) is skipped: the decoder consumes it, leaves the value at 0, and returns. | `lcd/utf8.cpp:157` | A corrupted or mis-sliced string silently loses a character rather than showing a replacement glyph. Slicing by byte offset rather than character can produce this. | open |
| 9 | A lead byte claiming more than four bytes (`0xFE`, `0xFF` — never valid UTF-8) is skipped the same way, with no indication. | `lcd/utf8.cpp:160` | As above. | open |
| 24 | **Null pointer dereference on `M502` with no card mounted.** `CardReader::jobRecoverFileExists()` calls `recovery.file.open(&root, …)` without checking `isMounted()`. With no volume mounted, `root`'s `SdVolume*` is null, `SdBaseFile::openRoot()` passes it straight to `SdVolume::fatType()`, and that dereferences null. `openJobRecoveryFile()` five lines below **does** have the `if (!isMounted()) return;` guard, so this is an omission in one of a pair, the same shape as #17. | `sd/cardreader.cpp:1677` (missing guard), crashes in `sd/SdVolume.h:107` | Reached by an ordinary user action: `M502` → `MarlinSettings::reset()` → `PrintJobRecovery::enable(false)` → `changed()` → `purge()` → `removeJobRecoveryFile()` → here. So **factory-resetting a printer that has power-loss recovery enabled and no SD card inserted dereferences a null pointer** — undefined behaviour, a hard fault on most targets. Requires `SDSUPPORT` + `POWER_LOSS_RECOVERY`, which is why the default test configuration never saw it. Found by building the media configuration for Phase 4b; it segfaults the test binary, which is what exposed it. | open |
| 22 | `static_assert(nullptr == strstr(EVENT_GCODE_SD_ABORT, "G27"), …)` is not a constant expression. `strstr` is only foldable when the compiler treats it as a builtin, which it does at `-O1` and above but not at `-O0`. | `inc/SanityCheck.h:449` | A configuration with media but without `NOZZLE_PARK_FEATURE` fails an unoptimised build with "non-constant condition for static assertion" instead of the message the check exists to print — so a user hits a compiler diagnostic about `strstr` rather than being told to enable nozzle parking. Optimised builds are unaffected, which is why it has survived: debug builds are the ones that break. Found while adding the media test configuration, which builds at `-O0`. | open |
| 23 | Enabling media auto-enables `REINIT_NOISY_LCD`, and `Warnings.cpp` then reports it as unnecessary on any board without an SD-detect pin. | `Configuration_adv.h:1842`, `inc/Warnings.cpp:1007` | Not wrong, but the default and the warning disagree: a board with media and no SD-detect always warns unless the user turns off an option they never turned on. Harmless where warnings are advisory; fatal where the build uses `-Werror`, which is how the test environments build. Worked around in `test/004-sd_powerloss.ini` rather than changed. | open |
| 17 | `wait_for_hotend()` sets `residency_start_ms = now + SEC_TO_MS(TEMP_RESIDENCY_TIME) / 3` on the *first* pass of the wait loop, then tests it with the three-argument `PENDING(now, start, interval)`, which is `(now - start) < interval` in unsigned arithmetic. A start time in the future wraps the subtraction to near 2^32, so the condition is false immediately and the loop exits. | `module/temperature.cpp:4846` (hotend), `:5039` (bed — an identical copy, found by mutation testing rather than by reading) | M109 does no residency wait at all when the hotend is already within TEMP_WINDOW of the target as the command begins, and M190 does the same for the bed against TEMP_BED_WINDOW — it returns on the next pass instead of holding for TEMP_RESIDENCY_TIME. Reach the window one pass later and the offset is zero, the subtraction does not wrap, and the full wait happens, so the effect is confined to a hotend that is already at temperature. Pinned by `simulated_temperature___M109_does_not_hold_a_hotend_that_is_already_at_the_target`. | open |

## Fixed, with reason

| # | Symptom | Where | Impact | Status |
|---|---|---|---|---|
| 16 | `RingBuffer::write()` incremented `index_write` before storing the byte, and `read()` incremented `index_read` before loading it, so each side advertised a slot it had not finished with. A reader seeing the new index before the store landed took the previous lap's byte from that slot; the real byte then overwrote a slot the index had already passed. | `HAL/LINUX/include/serial.h:54`, `HAL/TEST/include/serial.h:54` | One substituted character, same length, on any report longer than the 128-byte buffer — where `HalSerial::write()` spins and the producer genuinely races the consumer. Affects the simulator, which drains the buffer from its UI thread, and any ISR/main-loop pairing. | fixed |

**Why this one was fixed rather than recorded.** The rule exists so that adding tests
does not quietly change behaviour. Here the defect was *in the instrument*: it corrupted
the serial output the acceptance tests assert against, at roughly 1 run in 40. A suite
with a random failure cannot serve as a mutation baseline — every mutant is scored partly
by chance — so leaving it pinned by a characterization test would have meant building the
rest of the rescue on a measurement known to be unreliable.

Pinning it instead of fixing it was considered and rejected: a test asserting "output is
occasionally corrupted" is not a test, and the fault is not deterministic enough to pin.

Evidence, measured on an isolated worktree at `525d497f` with the same binary otherwise:
**6 failures in 200 runs unfixed, 0 in 400 fixed**. The control was run first, because a
clean run of the fixed binary proves nothing unless the harness is known to detect the
fault.

## Test harness defects

Not firmware. These are faults in the instrument, listed here so that a behaviour the
suite cannot reach is not mistaken for a behaviour the firmware does not have.

| # | Symptom | Where | Impact | Status |
|---|---|---|---|---|
| 42 | **PlatformIO swallows an AddressSanitizer report and prints a passing-looking summary beside it.** A sanitizer abort under `platformio test` surfaces as `Program received signal SIGHUP (Hangup)` and `[ERRORED]` — and on the same line as `331 test cases: 1 failed, 329 succeeded`, which is the count of everything that ran *before* the abort. Nothing in that output contains the word AddressSanitizer, the faulting function, or the address. This is the runtime twin of the documented build-time trap where a summary line is printed even when the build ERRORs. | `make unit-test-asan` | Not fixed — it is PlatformIO's output, not ours. The remedy is procedural and now written down: when the sanitizer run ERRORs, **run `.pio/build/testhal_native_asan/program` directly** and read stderr. This cost a wrong conclusion before it was understood: a sanitizer run that "found nothing" had in fact aborted several test files earlier and never reached the code under examination, and the empty result was read as evidence of cleanliness. A control that does not run is not a control. | recorded, procedural |
| 41 | **`MString::ltrim()` calls `strcpy` with overlapping source and destination.** `char *s = str; while (*s == ' ') ++s; if (s != str) strcpy(str, s);` — `s` points *inside* `str`, and `strcpy` with overlapping ranges is undefined behaviour; `memmove` is the defined equivalent. Reached by any `M550` whose new name has a leading space, and by anything else that trims an `MString`. Found by AddressSanitizer: `strcpy-param-overlap ... in MString<64, true>::ltrim()`, with `machine_name` named as the object. | `core/mstring.h:300` | Recorded, not fixed — it is firmware, not the instrument, and the standing rule is that changing what the firmware does is the maintainer's decision. It works today on glibc, which copies forward, and the ordinary suite cannot see it at all: the trim produces the right answer, so no assertion distinguishes it. **It does block the sanitizer**, which aborts here on every configuration with `CONFIGURABLE_MACHINE_NAME`, so `make unit-test-asan` cannot currently reach past `M550` under `009`. Verified by patching it to `memmove` locally: the run then continues to the end of the suite. That patch was reverted. | open, recorded not fixed |
| 40 | **A failing test leaves the instrument in a state the next test cannot survive — two ways, both silent.** Unity's failure path is a `longjmp` that unwinds no C++ stack, so a failing test skips its own tail *and* every destructor it was relying on. Two fixtures depend on a destructor for correctness and were therefore unsafe to fail inside. (1) `SerialCapture` marks a port `host_connected` and drains it from a second thread, restoring both in `~SerialCapture()`. Skipped, it leaves the worst combination possible: a port that busy-waits for room in 128 bytes and nothing emptying it. The next write of more than 128 bytes — from any later test, on any port that fixture touched — never returns. (2) A test that changes steps-per-millimetre and then fails leaves the machine at that scale, and scale decides how many pulses every later move costs. Both were found in one session and neither announces itself: the symptom is the suite failing to finish, in a test that has nothing to do with the cause. | `test/unit_tests.cpp`, `tests/gcode/serial_capture.h` | **Fixed** in `quiesce_simulated_peripherals()`, which runs on the far side of the `longjmp`: the ports are marked unattached and the axes restored to the scale the machine settled at. The scale baseline is *not* taken before the first test — `Planner::settings` is a zero-initialised static until a fixture calls `settings.reset()`, so recording there captures zeros and restoring them gives every axis an infinite millimetres-per-step, which hangs the suite exactly as the leftover it was meant to prevent. That mistake was made and observed here before being corrected. Both faults were found by injecting the fix for #33 and watching a *passing* suite become one that had to be killed at fifteen minutes. | fixed |
| 39 | **Panel buttons power up held down, on every simulated board.** Same family as the `KILL_PIN` trap already documented: encoder buttons are active-low with pull-ups on hardware, and every `Gpio` pin reads LOW at reset, so the firmware in a test build starts believing the click is held and both quadrature phases shorted — a state no encoder can physically be in. `Marlin::setup()` configures those pull-ups and does not run in a test build. | `test/unit_tests.cpp`, `tests/support/simulated_hardware.h` | **Fixed** — `SimulatedHardware::release_panel_buttons()`, called once before the first test and again after each one. What makes this worth an entry rather than a footnote is how it presented: nothing failed. A held knob changes what the UI does, not what any assertion looks at, so the suite stayed green and simply stopped being usable — the first configuration to define encoder pins at all went from **eight seconds to ten minutes**, and had to be killed. A harness fault that costs only time is harder to notice than one that costs a test. | fixed |
| 20 | The test suite's result depended on the order its objects were linked in, and `mutation_test.py:link_inputs()` collected them with `rglob('*.o')` — filesystem order, neither stable nor reproducible. Measured on `linux_native_test`: sorted order **segfaulted before the first test**, filesystem order **hung part-way through**. **Three independent causes, all now fixed.** (1) `all_marlin_tests` was a namespace-scope `std::list` in `unit_tests.cpp`, and every `MARLIN_TEST` registers into it from another translation unit's static initialiser — so any test initialised first pushed into an unconstructed list. Now constructed on first use. (2) `Clock::startup` was a class static with a dynamic initialiser; a caller reading the clock before `Clock.cpp` was initialised got a zero baseline, i.e. time since the epoch, which overflows once scaled. Now constructed on first use. (3) **The one that actually caused the hang:** `Stepper::init()` under this HAL arms a real POSIX interval timer whose handler is the stepper ISR, and the ISR programs the next interval as its last act, so it self-perpetuates. `Timer::disable()` only masks the signal — it leaves the timer armed — so a test that woke the stepper left a ~1 kHz signal source running for the rest of the process. Every later `sleep_for()` was then interrupted faster than it could complete (`si_overrun` 12-14, 780k signals in 25 s), and `Endstops::resync()`'s `safe_delay(2)` never returned. Added `Timer::stop()`, which **blocks the signal before disarming** — the other order leaves one queued signal to re-arm the timer it just stopped — plus `HAL_timer_stop_all()`, called from the test framework after every test. | `test/unit_tests.cpp`, `HAL/LINUX/hardware/Clock.{h,cpp}`, `HAL/LINUX/hardware/Timer.{h,cpp}`, `HAL/LINUX/timers.{h,cpp}`, `buildroot/share/scripts/mutation_test.py` | Mutation testing under `linux_native_test` works again, and `link_inputs()` now sorts, so a run is reproducible. Verified by linking the suite in **sorted, reversed, filesystem and four shuffled orders — all 377 pass in every one**. `crc16` re-measures at **97.1%**, exactly the figure recorded before the breakage. | fixed |
| 18 | `Timer::enable()` calls `schedule()`, which restarts the period: `next_fire_ns = now + period`. Real hardware does not — `HAL_timer_enable_interrupt()` sets an interrupt-enable bit and leaves the counter running, so a pending compare match still happens when it always would have. Under this HAL, anything that disables and re-enables a timer more often than that timer's own period starves it forever. `Stepper::endstop_triggered()` is exactly that: its `ATOMIC_SECTION_START/END` is a `suspend()`/`wake_up()` pair on MF_TIMER_STEP, and while a closed switch sits in front of a moving axis `Endstops::poll()` calls it from *every* temperature interrupt (~1 ms), against a step interval at the head of a block of ~2.8 ms. | `HAL/TEST/hardware/Timer.h:52` | A move that *begins* with its endstop already closed never completes: the abort the ISR was asked to perform is never carried out, `axis_did_move` is never cleared, and the next temperature interrupt asks again. Simulated time keeps advancing, so it presents as a hang, not a failure. A switch that closes *during* a move is unaffected, because by then the step interval is far shorter than the temperature period. The behaviour this costs is homing an axis that is already sitting on its switch — i.e. a second `G28` without `HOMING_BACKOFF_POST_MM`. **Fixed** by deleting `schedule()` from `enable()`. Now pinned the other way, by `endstops___a_move_that_starts_against_a_closed_switch_is_abandoned_at_once`. | fixed |

| 25 | ~~Executing a serial-received command hangs `GCodeQueue::advance()` in the `emergency_parser` + `advanced_pause_feature` configuration, once the line is long enough. Short lines — everything the suite already sends, up to roughly twenty characters — are fine; a 39-character line hangs, as does an 87-character one, with both a real command (`M117 <padding>`) and a string that is not a command at all. Not the over-long-line path: 39 characters is well inside `MAX_CMD_SIZE` (96). Not the receive buffer either: 88 bytes fits the port's 128. The hang is inside `advance()`, after the characters have been read and the command committed — narrowed to that by tracing either side of the call, not inferred. **Cause not established.** Found while writing a test for the over-long-line branch of the serial reader; that test was removed rather than left disabled. **Retried 2026-08-12 and it does not reproduce.** Nine shapes were sent through `get_available_commands()` under `002` — the named 39- and 87-character cases, a non-command string, a parenthesised comment, lines of 100 and 150 characters, and one on a port claiming a host with nothing draining it — under **both** the test HAL and the LINUX HAL. All nine complete. Which of the instrument fixes since (#20 link order, #32 the shared EEPROM file, or #40's leaked port state) removed it is *not* established, and the original diagnosis may simply have been wrong about the trigger; what is established is that the restriction it imposed no longer applies. | `gcode/queue.cpp`, reproduced under `test/002-extruders_1_runout.ini` | The restriction is lifted and the branch is asserted: `a_parameter_on_the_last_character_that_fits_is_still_read` and `an_over_long_line_still_runs_the_command_at_its_head`. Both validated by injection — cutting one character earlier is caught, and discarding the line instead of truncating it is caught. **Only the narrowing direction is testable, which is worth knowing:** the obvious companion assertion (the same parameter one character past the cut, expected to be lost) passes with the guard deleted, because without it the reader writes past the end of the buffer rather than reading more of the line. Every mutant that *widens* this guard is undefined behaviour, not different behaviour, and no assertion separates them. Also learned: a test cannot transmit more than the 128-byte receive buffer in one go — the newline is dropped with the rest and no command ever appears, which looks exactly like a refused line. | does not reproduce |

| 26 | **Two test fixtures with the same class name.** `simulated_heaters.h` already defined `SimulatedBed` — the heated bed, deriving from `SimulatedHeater` — and the new bed-*surface* fixture was given the same name. Both are defined entirely in headers with no key function, so each translation unit emits a weak COMDAT vtable under the same mangled symbol `_ZTV12SimulatedBed`, and the linker keeps one of them. Objects of the other class then carry method pointers belonging to the first: the vtable dump showed `SimulatedBed`'s `interrupt` and `update` slots holding `SimulatedHeater::interrupt` and `SimulatedHeater::update`, while its destructor slots were its own. So a step-pin write dispatched into heater code with `this` pointing at a bed surface, `curve()` built `float celsius[1024]` at whatever offset that landed on, and 4 KB went through the stack. **Fixed** by renaming to `SimulatedBedSurface` in `simulated_bed_surface.h`. | `tests/support/simulated_bed_surface.h`, `tests/support/simulated_heaters.h` | Probing works: `probe.cpp` 0% -> 75%, six tests. Nothing pointed at a name collision from the outside — the symptoms were a stack smash in an unrelated frame, an object whose destructor was emitted but never ran while its siblings in the same frame were destroyed normally, and an AddressSanitizer report naming a heater that no test had constructed. Every theory built to explain those as ordinary lifetime bugs was wrong. What settled it was dumping the vtable and finding another class's methods in it. | fixed |
| 27 | `types___SString` asserts the printed form of `1234.5 * 2345.602` and gets a different answer depending on how the suite is compiled: `2895645.67` under `-O2` and under `-O0` with coverage, `2895645.75` under AddressSanitizer. Both are correct roundings of the same product — the first is the nearest `double`, the second the nearest `float` — so what the assertion actually pins is whether the compiler folded the multiply at full precision before narrowing it. That is a property of the optimiser, not of `SString`. Upstream test, pre-existing; found by the first ASan run. The comparison was changed from `strcmp_P(...) == 0` to `TEST_ASSERT_EQUAL_STRING`, which prints both sides — the reason this took one build to diagnose rather than several. | `Marlin/tests/core/test_types.cpp:658` | The one known failure under `make unit-test-asan`. Everything else in the default configuration is clean under the sanitizer. | open, by-design elsewhere |
| 28 | **The Z clearance before homing is read as a coordinate when Z is unknown.** `G28` raises the nozzle by `Z_CLEARANCE_FOR_HOMING` before homing X and Y, so it is not dragged across the bed. The source states how the number should be read — "The 'height' for Z is a coordinate. But if Z is not trusted/homed make it relative" — but the condition is `!(z_min_trusted || axis_should_home(Z_AXIS))`, which asks for a relative lift only when Z **is** homed and its minimum is untrusted, and for an absolute coordinate when Z is unknown. `axis_should_home(Z)` is true when Z needs homing, so the comment's "not homed" case takes the branch meant for the "homed" one. | `gcode/calibrate/G28.cpp:413` | A machine whose believed Z is above the clearance and whose real Z is not makes no room at all, then homes X across the bed — the exact case the clearance exists for. Harmless from the usual power-on state, where the believed Z is 0 and a coordinate and a lift agree. Reachable after the steppers are released mid-job, or after any move by hand. Pinned by `homing_machine___an_unknown_Z_reads_the_clearance_as_a_coordinate_and_may_not_lift`, with `homing_lifts_a_nozzle_that_is_below_the_clearance` for the case where the two readings agree. | open, recorded not fixed |
| 35 | **Encoder steps/mm calibration left the reciprocal stale — confirmed, and fixed.** `I2CPositionEncoder::calibrate_steps_mm()` commands a known distance, asks the encoder how far the carriage really went, and rescales `axis_steps_per_mm` by the ratio. It assigned the array directly, so `mm_per_step` — the reciprocal the planner converts with — kept the value the calibration had just proved wrong. Measured: with the machine believing 80 steps/mm and the carriage really needing 100, calibration correctly reached 100 while `mm_per_step` stayed at 1/80. | `feature/encoder_i2c.cpp`, in `calibrate_steps_mm()` | **Fixed** — the write goes through `Planner::set_steps_per_mm()`, which owns the invariant. This is the same defect as the suspected #33 and #34 and the only one of the four that could be *confirmed*, because `007-i2c_encoders` and the simulated encoder made the file buildable and drivable. It is also the payoff argument for the encapsulation: the bug existed because the language could not express "you now owe a refresh", and it stopped existing the moment the field had an owner. Pinned by `calibration_leaves_the_reciprocal_agreeing_with_the_resolution`. | fixed |
| 34 | **An LCD driver refreshes one derived cache and not the other.** `mks_ui/draw_number_key.cpp` writes `axis_steps_per_mm` in five places and correctly calls `planner.refresh_positioning()` after each — then writes `max_acceleration_mm_per_s2` in ten more and never calls `refresh_acceleration_rates()` at all, which is what recomputes the acceleration limits in steps. Changing an acceleration limit from that menu therefore has no effect until something else refreshes. | `lcd/extui/mks_ui/draw_number_key.cpp:222-233` | Recorded, not fixed, and **not confirmed**: the file needs LVGL and is compiled by no configuration here. Sharper than #33 and the better argument for the encapsulation — this author *knew* the rule, applied it to one field, and missed the other, because nothing in the API says which fields owe a refresh. `Planner::override_max_acceleration()` makes it impossible to get wrong. **CONFIRMED by object-file inspection (2026-08-12).** Built for `mks_robin_nano_v1v2` and read with `arm-none-eabi-nm -uC draw_number_key.cpp.o`: the translation unit references `Planner::refresh_positioning()` and `Planner::settings`, and **no `refresh_acceleration_rates` and no `set_max_acceleration`**. It writes `planner.settings.max_acceleration_mm_per_s2[axis]` raw at `:222-228`. So it remembers to refresh the thing derived from steps-per-millimetre and forgets the thing derived from acceleration — the half-corrected version of #33, and the reason `Planner::set_max_acceleration()` keeps both in step. | open, outside the test frontier |
| 36 | **`sovol_rts.h`'s `sendData` overload set encodes the wire payload width in C++ type dispatch, and is ill-formed on any LP64 host.** `sendData(const int32_t n, ...)` forwards to the `uint32_t` overload (a 32-bit payload); `sendData(const int n, ...)` forwards to the `int16_t` one (16-bit). Where `int32_t` is `long` — arm-none-eabi, avr-libc — those are two distinct functions and the code is legal. On x86-64 Linux `int32_t` *is* `int`, so they are the same signature with different bodies, and the translation unit cannot compile. The obvious repairs all fail, **verified by reproducing the overload set under the target's typedefs and compiling it**: narrowing to `int16_t` redefines the overload already declared above it; narrowing to `int8_t` makes an `int` argument ambiguous; deleting it makes an `int` argument ambiguous. That overload exists to give `int` an exact match, which is the only thing holding the set together. The real correction is to stop dispatching wire width by type and make it explicit at the call sites — which changes what goes on the wire to a display if any call site is read wrongly, across a file no configuration compiles. | `lcd/sovol_rts/sovol_rts.h:281,287,288` | Recorded, not fixed. Blocked on being able to build and run the file at all; see the QEMU note in `CLAUDE.md`. The experiment that rules out the cheap fixes is cheap to repeat: reproduce the four overloads with `typedef long int32_t` and compile. | open, outside the test frontier |
| 37 | **`sovol_rts.cpp` cannot compile in any configuration, on any platform: it references four identifiers that do not exist.** `MARLINVERSION` (:902), `MACVERSION` (:1521), `SOFTVERSION` (:1522) and `CORP_WEBSITE_E` (:1523) each appear in the tree exactly once — at their use site — and are defined nowhere. Established by cross-compiling the real `GD32F103RET6_sovol_maple` environment with `arm-none-eabi-g++ 9.2.1`, having satisfied every configuration dependency the build asked for in turn (`FAN_SOFT_PWM`, media, power-loss recovery, a probe). The type-model collision of #36 never even arises on this target; these do. `ui.get_remaining_time()` is also called and is feature-gated, which is ordinary by comparison. The consequence is that this driver is **dead code in this repository** — no configuration builds it, no CI compiles it, and the behaviour recorded against it in #33 cannot run on any machine built from this tree. | `lcd/sovol_rts/sovol_rts.cpp:902,1521-1523,1602` | Recorded, not fixed — repairing it means inventing four constants and a display protocol's contents, which is authorship rather than a rescue. Supersedes the framing of #33 for this file and makes #36 secondary. No emulator helps: undefined identifiers fail at compile time, so QEMU has nothing to run. | open, dead code |
| 38 | **A declaration and its definition differ by a typedef, and agree on exactly one platform.** Three instances, found in one session, all the same shape. `dwin.h:223` declares `dwinDrawSignedFloat(..., long)` while `dwin.cpp:570` defines it taking `int32_t`; on arm-none-eabi `int32_t` *is* `long`, so they are one function and nobody notices, but anywhere `int32_t` is `int` they are two and a `float` argument is ambiguous between them. `HAL/TEST/include/Arduino.h` declared `delayMicroseconds(unsigned long)` against a definition taking `uint32_t` — different functions on LP64, an undefined reference the moment anything outside the usual configurations called it. And `sovol_rts.h`'s `sendData` pair (#36) is the same family. **Fixed** in the two cases where the header simply described the wrong function: the `dwin` declaration now says `int32_t`, and the HAL one `uint32_t`. The `dwin` change was proven inert on the shipping target by a controlled before/after build in one session — 126412 bytes of flash both ways. An earlier uncontrolled comparison across sessions showed an 8-byte difference and would have supported either conclusion. | `lcd/dwin/creality/dwin.h:223`, `HAL/TEST/include/Arduino.h`, `lcd/sovol_rts/sovol_rts.h` | The HAL one is an instrument defect and fixed on sight. The `dwin` one is a header corrected to match the definition that already existed, which is not a behaviour change on any platform. #36 is not fixed, because there the two functions are both real and differ in what they do. | 2 fixed, 1 open (#36) |
| 33 | **Two LCD drivers set steps-per-millimetre without refreshing what is derived from it.** `mm_per_step` is the reciprocal of `axis_steps_per_mm` and the stepper counts in steps, so both are invalidated when a resolution changes; `refresh_positioning()` recomputes them. `sovol_rts.cpp` (the `Steps_X/Y/Z/E` cases of `RTS::handleData()`) and `creality/dwin.cpp` (`hmiStepXYZE()`) assign the array from their menus, and **neither file contains a single call to `refresh_positioning()`**. Between such an edit and the next thing that happens to save settings, the machine plans at the new resolution and steps at the old one. | `lcd/sovol_rts/sovol_rts.cpp:1322-1346`, `lcd/dwin/creality/dwin.cpp:1647` | **CONFIRMED for `creality/dwin.cpp` by object-file inspection (2026-08-11).** Built for `STM32F103RE_creality` and read with `arm-none-eabi-nm -uC dwin.cpp.o`: the translation unit references `Planner::set_max_feedrate(AxisEnum, float)` and `Planner::set_max_acceleration(AxisEnum, float)` — the setters that *do* refresh what is derived — and `Planner::settings`, the raw struct. It references **`refresh_positioning` zero times, `set_steps_per_mm` zero times, and no symbol containing `refresh` at all**. So the file is not uniformly careless: it uses the proper setter for two of the three quantities and writes the third straight into the array (`:1647`). That is the strongest possible form of the argument for the encapsulation — the driver used an API wherever one existed, and got it wrong precisely where one did not. Recorded, not fixed. **Half of the original entry is now moot and the other half is confirmed.** `sovol_rts.cpp` cannot compile on any platform (#37), so its version of this mistake cannot run on a machine built from this tree. `creality/dwin.cpp` **does** cross-compile clean for `STM32F103RE_creality`, so its version is live code and the suspicion can be pursued — by reading with a compiler that accepts the file, and eventually by running it. What was checked originally is that neither file calls `refresh_positioning()` and that the writing paths do not adjacently call `settings.save()`; What was checked is that the files never call `refresh_positioning()` and that the writing paths do not adjacently call `settings.save()` (which would refresh via `postprocess()`); what was not checked is whether some other route reaches a refresh first. Found while encapsulating the field — `Planner::set_steps_per_mm()` makes this class of mistake impossible for callers that use it, which is the argument for retiring the public array once these consumers are covered. | open, outside the test frontier |
| 32 | **The emulated EEPROM was file-backed, shared and order-dependent — an instrument defect, fixed.** `HAL/TEST/eeprom.cpp` kept the store in `eeprom.dat` in the working directory. Three faults: `access_start()` returned **false** when the file did not exist, so the first `M500` of a run silently did nothing on a clean checkout and the suite only passed from the second run onwards; a file shorter than the store was memset from `file_size` onward *without ever reading the part that existed*; and the file is shared by every process using that working directory, which `mutation_test.py` runs up to one per core with `cwd=REPO`. | `Marlin/src/HAL/TEST/eeprom.cpp` | Fixed, per the precedent of #16/#18/#20 that defects in the measuring instrument are corrected rather than recorded. The test store is now process-local memory that starts erased, which is what a board with never-written EEPROM reads as — `settings.cpp` sees the same bytes through the same API, and the suite gains isolation between processes. `HAL/LINUX/eeprom.cpp` keeps its file, because the simulator wants settings to survive a restart, but has the two read faults fixed. | fixed |
| 31 | **`M92 E<low>` compensated the acceleration limit and the compensation was immediately discarded.** A suspiciously low E resolution (below 20 steps/mm) is treated as a change of units rather than of speed, and `M92.cpp` scaled both `settings.max_feedrate_mm_s[E]` and `planner.max_acceleration_steps_per_s2[E]` by the ratio. The second array is derived: `refresh_acceleration_rates()` recomputes every entry from `max_acceleration_mm_per_s2 * axis_steps_per_mm`, unconditionally and in every configuration, so the write was overwritten before anything could read it. | `gcode/config/M92.cpp` | **Fixed** — the dead write is deleted. Not a behaviour change: removing it leaves the whole suite passing, which is how it was confirmed both when found and when removed. It was not *repaired* because what it intended is unrecoverable — the factor it applied preserves neither the mm/s^2 figure nor the steps/s^2 one. The feedrate half is the real compensation and is kept, holding the extruder speed limit constant in steps per second. Behaviour pinned by `the_low_M92_E_acceleration_compensation_is_discarded`. | fixed |
| 30 | **The cold- and lengthy-extrusion guards are implemented twice, and the outer one is therefore untestable.** `motion.cpp:1953-1980` refuses an extrusion through a cold nozzle or one longer than `EXTRUDE_MAXLENGTH`, reports it, and advances the firmware's idea of the filament position so the host stays in sync. `planner.cpp:1850-1876` does all three again, with the same two messages. Whichever runs first makes the other a no-op, so disabling the `motion.cpp` guard entirely leaves every assertion satisfied — the planner emits the identical string. | `module/motion.cpp:1953`, `module/planner.cpp:1850` | Not a fault: the planner's guard is the backstop for `buffer_line()` callers that bypass `prepare_line_to_destination()`, so both are wanted. It does mean ~12 mutants across `:1953`, `:1958` and `:1959` have no observable outcome, including a wrong `e_delta` formula — `/ e_factor` instead of `* e_factor` survives every test because the planner recomputes it correctly. Distinguishing them needs an assertion on which layer refused, which nothing currently exposes. | open, redundant by design |
| 29 | **The small-segment junction cap is reachable but masked.** `JD_HANDLE_SMALL_SEGMENTS` caps the junction speed of short, nearly-collinear moves at what an equivalent circular arc allows — `v² = a·r`, radius recovered as chord over turn angle — because junction deviation permits an absurd speed through a slight corner. Junction deviation is a function of the corner *angle* alone, and with `JUNCTION_DEVIATION_MM` at its default its limit is the lower of the two for every radius the machine can traverse: above roughly 0.55 mm the arc cap is larger and never binds, and below it the whole circle is too short to reach any cap. | `module/planner.cpp:2594-2679` | Not a fault — the cap is a safety net that this configuration does not need. It means ~62 mutants on those lines have no observable outcome *here* rather than being unasserted: the value is computed and then discarded by the `NOMORE`. A build with a larger `JUNCTION_DEVIATION_MM`, or one reaching much smaller radii, would make it bind. Found by writing a test that asserted the cap and passed with the branch compiled out. | open, masked by configuration |

**Why this one was fixed rather than recorded.** Same reasoning as #16: the defect was in
the instrument, not the firmware. `HAL/TEST` is compiled only into the `testhal_*`
environments and ships to nobody, and its entire purpose is to behave like the hardware
it stands in for — so a divergence from hardware semantics is not behaviour to preserve,
it is the instrument being wrong. It also cost real coverage: homing an axis already on
its switch was unreachable, and presented as a hang rather than a failure.

Recording it instead was considered and rejected. A characterization test would have
pinned a livelock, and "this move never completes" is not behaviour any firmware
maintainer would want protected.

The test that pinned the stall carried a message saying what to do when the stall was
fixed, which is why the change surfaced immediately rather than silently passing.

## Recorded as intended

These follow from fixed-width display fields and are consistent across the library. They
are listed so that a future change knows it is changing something deliberate.

| # | Behaviour | Where |
|---|---|---|
| 10 | A value too wide for its field is truncated to the low digits with no marker: `ui16tostr3rj(1000)` is `"000"`, `ui8tostr2(100)` is `"00"`, `ftostr72rj(123456.7)` is `"23456.70"`. | `libs/numtostr.cpp` |
| 11 | The `ns` (no sign) conversions drop the sign, so `-1.5` renders identically to `1.5`. Likewise `ftostr5rj`. | `libs/numtostr.cpp` |
| 12 | `pcttostrpctrj()` does not clamp, so a value above 100 prints as-is (`123%`). | `libs/numtostr.cpp:73` |
| 13 | `ftostr52sprj()` and `utostr3()` *clamp* at their maximum while everything else truncates — an inconsistency within the library, not a fault in either. | `libs/numtostr.cpp` |

## Design corrections, blocked

Not defects; structural problems whose fix is gated on the test frontier
(see `CLAUDE.md`).

| # | Correction | Blocked until |
|---|---|---|
| 14 | `GCodeParser` is static-only with global mutable state (`codenum`, `codebits`, `string_arg`), read directly across the codebase. | its consumers are rescued — Phase 2 |
| 15 | Every `numtostr` conversion writes into one shared `char conv[9]` and returns a pointer into it, so two calls in one expression clobber each other. Fan-in of 23 files. | its consumers are rescued |
| 21 | **The HAL is selected by build-time file substitution, not by dependency inversion.** A platform is chosen with `-D__PLAT_*` and a `build_src_filter` that compiles one `src/HAL/<PLATFORM>` directory, and the firmware reaches hardware through macros (`WRITE`, `READ`, `HAL_timer_*`) that are textual substitution. So the HAL is not a dependency that can be injected — it is a directory swapped at build time, which is why exactly one can exist per binary, why each new platform is a wholesale rewrite of ~30-50 files, and why the real HALs (AVR, STM32, …) have no tests at all: nothing can stand in for them. Wanted instead: compile-time dependency inversion — the modules take their hardware access as a template parameter, so a real and a fake can be instantiated side by side in one binary at no runtime cost. | its consumers are rescued — this is every module that touches hardware, so it is the largest entry here. See the sequencing note below. |
| 19 | The thermal limit checks have no observable outcome other than shutting the machine down. `Temperature::_temp_error()` ends in `marlin.kill()`, whose last act (`minkill()`) is `while (!kill_state()) hal.watchdog_refresh();` — a spin waiting for an operator to press the kill button, with `watchdog_refresh()` a no-op and nothing able to change the pin from inside the loop. So no test can drive a sensor out of range and then *assert* anything: the call does not return. Measured rather than assumed — a probe driving `TEMP_0_PIN` to raw 1023 with a target set hangs the binary. The consequence is that `updateTemperaturesFromRawValues()`'s MINTEMP/MAXTEMP checks (`temperature.cpp:2973-2999`) can only be detected by a mutant *hanging*, never by a value. What is wanted is a seam that lets the shutdown be observed and returned from under test — the same shape as the `killed` flag the function already keeps, but reachable. That is a change to a production surface `kill()`'s many callers depend on. | `MarlinCore.cpp` and `temperature.cpp` are rescued — Phase 4a item 4 |

### Sequencing note for #21 — inverting the HAL

Two constraints shape any design, both measured rather than assumed.

**The language level is set by the oldest target, not the newest.** AVR builds with
`-std=gnu++1z` (C++17) on the avr-gcc that PlatformIO ships, and several platforms
`build_unflags` down to `gnu++11`/`gnu++14`. **C++20 concepts are therefore not available
on the targets that most need the abstraction.** That is not fatal: compile-time
dependency inversion at zero runtime cost is fully expressible in C++17 with class
templates, policy parameters, CRTP where static dispatch must go the other way, and
`static_assert` to check a policy's shape. Concepts are the ergonomic layer — better
error messages and named requirements — and can be added later, behind a feature test
macro, on platforms whose toolchain allows them. Designing *around* concepts today would
either exclude AVR or force a toolchain decision that has nothing to do with testability.

**It cannot be one change.** #21's consumers are every module that touches hardware, and
the ordering rule in `CLAUDE.md` forbids editing untested call sites. The migration that
respects it:

1. Add the templated seam **alongside** the existing macros. Purely additive — no call
   site moves, nothing is blocked by anything.
2. Redefine the macros as thin forwards to a default instantiation, so the existing call
   sites keep working unchanged and the two paths cannot diverge.
3. Migrate one module at a time, and only once *that module* is covered and mutation
   tested. The gate is per-module, not global: the whole firmware does not have to be
   rescued before any of it can move.
4. A platform HAL becomes a policy type rather than a directory, at which point a real
   and a fake can be instantiated in the same binary.

Step 4 is what makes the real HALs testable, and it would also retire the
`testhal`/`linux` split recorded in the plan document: a fake would no longer need its
own build environment, because it would no longer need to be the *only* HAL in the binary.

## Working on one of these

1. The pinning test already exists and passes against current behaviour. Start there.
2. Invert it: assert the intended behaviour, watch it fail.
3. Fix, then remove the `LEGACY-BEHAVIOR:` comment and the entry here.
4. Check the call sites — several of these are latent only because callers happen to
   avoid the input that triggers them.
